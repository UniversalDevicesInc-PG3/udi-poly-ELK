<editors>
  <!-- Boolean -->
  <editor id="bool">
      <range uom="2" subset="0,1" />
  </editor>
  <editor id="onoff">
      <range uom="78" subset="0,100,101" />
  </editor>
  <editor id="I_DEBUG">
    <range uom="25" subset="9,10,20,30,40,50" nls="CDM"/>
  </editor>
  <editor id="integer">
       <range uom="25" min="0" max="1000" prec="0" />
  </editor>
  <editor id="ontime">
       <range uom="58" min="0" max="65535" prec="0" />
  </editor>
  <editor id="onoff">
       <range uom="78" subset="0,100,101" prec="0" />
  </editor>

  <!-- AREAS -->

  <!-- Arm Status -->
  <editor id="astatus">
      <range uom="25" min="-1" max="10" nls="ASTATUS" />
  </editor>
  <editor id="astatusc">
      <range uom="25" min="0" max="10" nls="ASTATUS" />
  </editor>
  <editor id="armup">
      <range uom="25" subset="-1,0-6" nls="ARMUP" />
  </editor>
  <editor id="alarm">
      <range uom="25" subset="-1,0-18" nls="ALARM" />
  </editor>
  <editor id="chime">
      <range uom="25" subset="-1,0-7" nls="CHIME" />
  </editor>

  <!-- KeyPads -->

  <!-- Temperature -->
  <editor id="I_TEMP_C">
    <range uom="4" min="-50" max="75" step="0.5" prec="1" />
  </editor>
  <editor id="I_TEMP_F">
    <range uom="17" min="-50" max="150" step="0.5" prec="1" />
  </editor>
  <!-- Status -->
  <editor id="I_LAST_USER">
      <range uom="25" subset="0-203" nls="USER" />
  </editor>

  <!-- ZONES -->

  <!-- Status -->
  <editor id="zpst">
      <range uom="25" subset="-1,0-3" nls="ZPST" />
  </editor>
  <!-- Status -->
  <editor id="zlst">
      <range uom="25" subset="-1,0-3" nls="ZLST" />
  </editor>
  <!-- Status -->
  <editor id="area">
      <range uom="56" subset="0-15" />
  </editor>
  <!-- Status -->
  <editor id="ztype">
      <range uom="25" subset="0-36" nls="ZTYPE" />
  </editor>
  <!-- Status -->
  <editor id="zalarm">
      <range uom="25" subset="0-25" nls="ZALARM" />
  </editor>
  <!-- ON Off Config -->
  <!-- <editor id="zdconf">
      <range uom="25" subset="0-6" nls="ZDCONF" />
  </editor>-->
  <!-- Send On/Off -->
  <editor id="zsonoff">
      <range uom="25" subset="0-3" nls="ZSONOFF" />
  </editor>
  <!-- Zone Voltage -->
  <editor id="zvolt">
    <range uom="72" min="0" max="13.8" step="0.1" prec="1"/>
  </editor>
</editors>
